/**
 * Local Maps API key (not committed).
 * Canonical copy: `templates/maps-config.example.js` — keep in sync when changing.
 *
 * 1. Copy this file to maps-config.js (same folder).
 * 2. Replace YOUR_GOOGLE_MAPS_API_KEY with your browser-restricted key.
 * 3. maps-config.js is listed in .gitignore — do not commit it.
 *
 * https://developers.google.com/maps/documentation/javascript/get-api-key
 */
window.__GMAPS_KEY__ = "YOUR_GOOGLE_MAPS_API_KEY";
