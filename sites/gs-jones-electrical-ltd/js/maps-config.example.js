/**
 * Google Maps browser API key (drag-and-drop: rename/copy this file to maps-config.js).
 *
 *   1. Copy this file to `js/maps-config.js`
 *   2. Replace YOUR_GOOGLE_MAPS_API_KEY with your browser-restricted key
 *   3. Re-upload the site folder to Netlify
 *
 * Git deploys: `maps-config.js` is gitignored — use Netlify snippet injection instead,
 * or add maps-config.js only locally before drag-drop (never commit the real key).
 *
 * https://developers.google.com/maps/documentation/javascript/get-api-key
 */
window.__GMAPS_KEY__ = "YOUR_GOOGLE_MAPS_API_KEY";
